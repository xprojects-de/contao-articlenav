{% block articlenav %}

    {% add "articlenav_js" to head %}
        <script src="{{ asset('articlenav.js', 'articlenav') }}" type="module"></script>
    {% endadd %}

    <nav class="{{ class }}" id="{{ containerId }}">
        <ul data-controller="articlenav">

            {% for item in items %}

                <li>
                    <a href="#" class="articlenavitem" data-action="click->articlenav#navigate"
                       data-articlenav-href-value="{{ item.href }}" data-articlenav-ident-value="{{ item.articleid }}"
                       data-articlenav-alias-value="{{ item.alias }}" title="{{ item.title }}">
                        {{ item.text }}
                    </a>
                </li>

            {% endfor %}

        </ul>
    </nav>

{% endblock %}